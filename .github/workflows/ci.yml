name: simple CI pipeline

on:
push:
branches: ["main"]

jobs:
build:
runs-on: ubantu-latest
steps:
- name: checkout repository
uses: actions/checkout@v3

-name: run a simole command
run: echo "this is ubantu"